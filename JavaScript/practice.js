let pencilPrice = 10;
let eraserPrice = 5;
// let total = pencilPrice + eraserPrice;
// let output = `Total price is ${pencilPrice + eraserPrice}`;
// let output2 = "Total price is:"+ (pencilPrice + eraserPrice) + " rupees"; 
console.log(`Total price is  ${pencilPrice + eraserPrice}`);

// let password = prompt("Enter your password");
// let newpassword = password.trim();  trim the spacearound the string
// console.log(newpassword);

// Array
let fruits = ["apple", "banana", "mango", "orange"];
fruits.push("grapes");
fruits.pop();
fruits.unshift("kiwi"); // add element at the beginning
fruits.shift(); // remove element from the beginning

console.log(fruits);
fruits.indexof("mango"); // find the index of the element

// let marks = [10, 20, 30, 40, 50];
// console.log(marks); // 20, 30
// marks.indexOf(30); // 2
// marks.includes(30); // true
// let all=fruits.concat(marks); // combine two arrays
// let all=fruits.concat(marks); // combine two arrays
// console.log(all);


// fruits.slice(1, 3); // 1,2 

// fruits.splice(1, 2); // 1,2 remove 2 elements from index 1


// fruits.sort(); // sort the array
// fruits.reverse(); // reverse the array

// let numss = [[1, 2], [3, 4], [5, 6]];

// (3) [Array(2), Array(2), Array(2)]
// 0
// (2) [1, 2]
// 1
// (2) [3, 4]
// 2
// (2) [5, 6]
// length
// 3
// [[Prototype]]
// Array(0)
